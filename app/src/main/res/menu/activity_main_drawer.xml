<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_quick" android:icon="@android:drawable/ic_menu_camera"
            android:title="@string/menu_title_quick_location" />
        <item android:id="@+id/nav_exact" android:icon="@android:drawable/ic_menu_gallery"
            android:title="@string/menu_title_exact_location" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item android:id="@+id/nav_share" android:icon="@android:drawable/ic_menu_share"
                android:title="Share" />
            <item android:id="@+id/nav_send" android:icon="@android:drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>